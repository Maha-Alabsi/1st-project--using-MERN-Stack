import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#f5f5f5',
      contrastText:'#434343',
    },
    secondary: {
      main: '#c8a165',
    },
    tertiary: {
      main: '#c87065',
    },
  },
});

// f5f5f5 
export default theme;
