import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles({
  gridHeight: {
    height: '200px',
  },

  tFieldColor: {
    color: 'white',
    width: '400px',
  },
});

export default useStyles;
