import { makeStyles } from '@material-ui/core/styles';
const comStyles = makeStyles({
    reactLink: {
        textDecoration: 'none',
        color: '#434343',
        '&:hover': {
          color: '#c8a165',
        },
    }
});

export default comStyles;
